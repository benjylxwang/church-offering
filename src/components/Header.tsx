import { Box, Typography } from "@mui/material";

function Header() {
  return (
    <Box sx={{ backgroundColor: "turquoise" }}>
      <Typography variant="h1">Sunday Offering</Typography>
    </Box>
  );
}

export default Header;
